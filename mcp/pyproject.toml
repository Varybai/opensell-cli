[build-system]
requires = ["maturin>=1.5,<2"]
build-backend = "maturin"

[project]
name = "opensell-mcp"
version = "0.3.2"
description = "OpenSell MCP Server for C2C agentic commerce"
requires-python = ">=3.8"

[tool.maturin]
bindings = "bin"
manifest-path = "Cargo.toml"
