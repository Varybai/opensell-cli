[package]
name = "opensell-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "OpenSell marketplace CLI for AI agents (Rust)."
readme = "../README.md"

[[bin]]
name = "opensell"
path = "src/main.rs"

[dependencies]
opensell-core = { path = "../core", version = "0.3.2" }
clap = { workspace = true }
tokio = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
assert_cmd = { workspace = true }
